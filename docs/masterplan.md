# 🏗️ Monewment B2B SaaS Master Plan

> **Version:** 1.2 (2026-01-04)
> **Status:** Active Development
> **Goal:** 기업(ProjectClient)이 AI DevOps 엔진을 구독하고, 팀원(ProjectMember)을 초대하여 **권한별로 협업**하는 엔터프라이즈 멀티테넌트 플랫폼 구축

---

## 1. 핵심 권한 구조 (RBAC)

시스템 내 사용자들의 역할과 권한 범위입니다.

| Role | 정의 | 권한 범위 | 비고 |
| :--- | :--- | :--- | :--- |
| **SuperAdmin** | 플랫폼 소유자 | 전체 수익 관리, 시스템 로그 감시, 기능 잠금/해제 | 플랫폼 운영자 |
| **Organization** | 법인격 (Team/Brand) | **결제의 주체**. 프로젝트를 소유함. | 예: 삼성전자 AI팀 |
| **ProjectAdmin** | 프로젝트 관리자 | 프로젝트 생성/삭제, 결제 관리, **팀원 초대 및 권한 부여** | 프로젝트 오너 |
| **ProjectMember** | 실무자 (Worker) | 초대받은 프로젝트 내에서 **허용된 기능(Allowed Features)**만 사용 가능 | 일반 팀원 |

---

## 2. 사용자 시나리오 (User Flow)

### ✅ Phase 1: 진입 및 온보딩 (기반 구축 완료)
1.  **접속 (Landing):** 사용자가 플랫폼(`localhost:3000`)에 접속.
2.  **인증 (Auth Guard):** 로그인/회원가입 프로세스 통과.
3.  **엔진 생성 (Provisioning):**
    * `[+ Create Engine]` 버튼 클릭.
    * 프로젝트명(ID)과 법인명(Org) 입력.
    * 시스템이 물리적 폴더 및 DB 레코드 생성.

### ✅ Phase 2: 서비스 확장 및 팀 관리 (핵심 기능 구현됨)
4.  **통합 설정 진입:** 대시보드 상단 `[⚙️ 통합 설정]` 버튼 클릭.
5.  **서비스 관리 (Service):**
    * `[Service]` 탭에서 설치된 API 기능 확인.
    * 마켓플레이스에서 유료 기능(AI Bot 등) 확인 및 가상 구매.
6.  **팀원 관리 (Team Collaboration):**
    * `[Team]` 탭에서 이메일로 동료 초대.
    * 초대된 동료에게 'Logs', 'MCP Bot' 등 **세부 권한(Feature Key)** 부여.
    * 관리자(Admin)와 일반 멤버(Member) 구분 표시.
7.  **시스템 인텔리전스 (System Intelligence) - [기능 강화 완료]:**
    * `[General]` 탭 하단에서 **실시간 DB 스키마** 및 **API 명세** 자동 수집/시각화.
    * **[New] CCTV 모듈(Watchdog):** 파일 변경 감지 시 `docs/auto_generated` 내 4대 핵심 문서(구조, 데이터, 통신, 설정) 자동 최신화.
    * `.env` 환경 변수 에디터 제공.

### 🚧 Phase 3: 운영 및 AI 제어 (개발 예정)
8.  **상세 제어 (Control Center):**
    * 대시보드의 `[상세 관리]` 클릭 시 **개별 프로젝트 페이지**로 진입.
    * 프로젝트별 전용 URL (`/projects/[name]`) 라우팅.
9.  **실시간 로그 & AI (Live Ops):**
    * 실제 터미널(`main.log`) 로그를 웹으로 실시간 스트리밍.
    * 에러 발생 시 AI 에이전트가 자동 감지 및 해결책 제안 채팅.

---

## 3. 기술 스택 (Tech Stack)

| 구분 | 기술셋 | 역할 |
| :--- | :--- | :--- |
| **Frontend** | Next.js 14+ (App Router) | UI/UX, 대시보드, 설정 페이지, 로그인 세션 관리 |
| **Backend** | FastAPI | REST API, Auth(JWT), 파일 시스템 제어, 프로세스 관리 |
| **Database** | SQLite (SQLAlchemy) | 사용자/프로젝트/팀원 정보 저장 (추후 PostgreSQL 마이그레이션 가능) |
| **Module** | Python `multiprocessing` | 개별 프로젝트 엔진 독립 실행 및 제어 |
| **DevOps** | **Python `watchdog` & Custom Inspectors** | **[New]** 실시간 파일 감시, 프로젝트명 자동 인식, 문서 자동화(CCTV) |
| **Intel** | `src.core.devtools` | **[New]** 시스템 자가 진단 및 메타데이터 자동 수집기 (Data, Network, Structure, Config) |

---

## 4. 심층 개발 로드맵 (Deep-Dive Checklist)

### ✅ Priority Zero: 개발 환경 자동화 (Infrastructure Completed)
*개발 효율성 극대화 및 AI 협업을 위한 인프라 구축 완료.*

- [x] **Z-1. 범용 프로젝트 실행기 (`start_project.ps1`)**
    - [x] 폴더명 기반 프로젝트 이름 자동 감지 구현.
    - [x] Docker, Backend, Frontend, CCTV 통합 실행 지원.
- [x] **Z-2. 지능형 감시 시스템 (CCTV)**
    - [x] `scripts/run_local_cctv.py` 경로 인식 문제 해결 및 이사 완료.
    - [x] 4대 엔진(`inspectors`) 모듈화 및 `src/core/devtools` 통합.
    - [x] 파일 변경 시 `docs/auto_generated/` 내 MD 파일 실시간 갱신 구현.
- [x] **Z-3. 좀비 폴더 및 중복 파일 제거**
    - [x] 구버전 스크립트(`inspector.py`, `start_monewment.ps1` 등) 삭제 및 최적화.
    - [x] 잘못 생성된 중복 폴더(`monewment/src`, `projects/Monewment`) 제거 완료.

### 🚀 Priority A: 동적 라우팅 및 상세 페이지 (Urgent)
*단일 프로젝트 하드코딩을 제거하고, 다중 프로젝트를 지원하는 구조로 전환.*

- [ ] **A-1. 동적 설정 페이지 개편**
    - `gui/app/settings/page.tsx` → `gui/app/projects/[name]/settings/page.tsx`로 이동.
    - URL 파라미터(`[name]`)를 읽어 해당 프로젝트의 정보만 로드하도록 수정.
- [ ] **A-2. 프로젝트 상세(Control Center) 페이지 생성**
    - `gui/app/projects/[name]/page.tsx` 신규 생성.
    - 실시간 로그 뷰어(Log Stream) 및 AI 채팅창 배치.
- [ ] **A-3. 대시보드 링크 연결**
    - 대시보드 카드의 `[상세 관리]` 버튼과 A-2 페이지 라우팅 연결.

### 🧠 Priority B: AI 엔진 실가동 (Core Value)
*Gemini API Key를 활용하여 실제 AI 분석 기능을 활성화.*

- [ ] **B-1. 로그 파일 스트리밍 API 구현**
    - 백엔드: `main.log` 파일을 실시간(Tail)으로 읽어주는 SSE/Polling API 최적화.
    - 프론트엔드: 터미널 스타일의 로그 뷰어 UI 구현.
- [ ] **B-2. AI 분석 요청 연결**
    - 프론트엔드: 로그 뷰어 옆 `[AI 분석]` 버튼 생성.
    - 백엔드: 저장된 암호화 키를 복호화하여 Google Gemini API 호출 및 결과 반환.
- [ ] **B-3. 에러 자동 감지 시스템**
    - 로그 스트림에서 `Error`, `Exception` 키워드 감지 시 UI 알림(Red Alert) 발생.

### 🛡️ Priority C: 보안 및 권한 검증 (Security)
*UI 상의 권한이 실제 API 호출 시에도 적용되도록 보안 강화.*

- [ ] **C-1. API 권한 미들웨어(Dependency) 구축**
    - API 호출 시 `ProjectMember` 테이블을 조회하여 권한(`allowed_features`) 검증 로직 추가.
    - 권한 없음 시 `403 Forbidden` 리턴.

---
*Document generated by Monewment AI Assistant.*